                            -----THIS IS ALX-SE TEAM OF COMMAND DICTIONARY-----


     * pwd = print working/current directory or folder.

     * cd  = change directory ( this command only for directory ).

           # in cd command directory can be specified two different way 'absolute and relative'.

	      *Absolute - from the root directory and leads to its destination.

	      *Relative - a relative pathname starts from the working directory to the home directory.

	   # if we type cd followed by nothing, cd will change the working directory to our home directory.   

     * ls  = list files and directory.

         * ls -a = list also hidden files and directorys.

     	 * ls -l = list file and directory in long format.

     * less= it is a program that lets us view text file mean content of file.
